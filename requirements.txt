# Runtime dependencies, pinned to the versions the deploy build was validated
# against (resolved on Python 3.12 in CI). The playwright pin also fixes the
# Chromium build that `playwright install chromium` downloads in netlify/build.sh.
beautifulsoup4==4.15.0
requests==2.34.2
flask==3.1.3
pypdf==6.13.2
pdfplumber==0.11.10
PyJWT==2.13.0
playwright==1.60.0
# Pillow: compress Helio thumbnails to WebP for the committed thumbnail cache
# (scripts/cache_helio_thumbnails.py). Build-time only; small wheel.
Pillow==11.3.0
