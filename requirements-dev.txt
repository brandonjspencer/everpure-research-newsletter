# Dev + CI tooling, pinned to the versions this workflow was validated against.
# Keep `ruff` in sync with the `rev` in .pre-commit-config.yaml.
ruff==0.15.17
pyright==1.1.410
pytest==8.4.2
pre-commit
